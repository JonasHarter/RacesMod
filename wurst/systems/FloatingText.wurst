package FloatingText
	constant real DUR = 1.0
	constant real FSIZE = 10.0
	constant vec2 V = vec2(0.0, 0.05)
	
	public function createFloatingText(vec3 pos, string text, color c)
		createFloatingText(pos, text, c.withAlpha(255))
		
	public function createFloatingText(vec3 pos, string text, colorA c)
		createTTEx(pos, V, text, FSIZE, DUR, c)
		
